[package]
name = "common"
version = "0.1.0"
edition = "2021"
publish = false
description = "Shared access-control (RBAC) and error-handling primitives for Fund-My-Cause Soroban contracts"
readme = "README.md"
license = "MIT"

[lib]
doctest = false

[features]
# Exposes `test_utils` to downstream contract test suites.
testutils = ["soroban-sdk/testutils"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }

# Inherit the workspace-level clippy pedantic configuration (issue #1162).
# Contract-specific exceptions are documented with #[allow] in the source.
[lints]
workspace = true
