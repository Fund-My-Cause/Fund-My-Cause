node_modules
.next
dist
target
*.wasm
.env
.env.local
.env.local.backup
# Cargo.lock is intentionally tracked — see README
task1.md

# Contract coverage (Issue #1170)
# The HTML report is large and reproducible — don't commit it.
# coverage/summary.txt IS committed so reviewers can see trends in PRs.
coverage/html/
coverage/lcov.info

# Test fixtures (generated)
fixtures/seed-data.json
fixtures/test-fixtures.json
fixtures/archive/
contracts/achievements/test_snapshots/
contracts/crowdfund/test_snapshots/
contracts/registry/test_snapshots/
.claude/worktrees/
**/test_snapshots/coverage/
